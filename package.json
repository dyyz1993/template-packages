{
  "name": "{{name}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "main": "index.js",
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "commit": "git-cz",
    "preinstall": "npx only-allow pnpm",
    "pnpm:build":"pnpm run -r build ",
    "pnpm:publish":"pnpm run -r publish ",
    "pnpm:publish:dry":"pnpm run -r publish --dry-run",
    "lint": "eslint --fix --ext .ts,.js packages",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "{{author}}",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^20.3.1",
    "@typescript-eslint/eslint-plugin": "^5.60.0",
    "@typescript-eslint/parser": "^5.59.11",
    "@zeronejs/cli": "^3.2.2",
    "eslint": "^8.42.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-import": "^2.27.5",
    "eslint-plugin-prettier": "^4.2.1",
    "prettier": "^2.8.8",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.1.3"
  }
}
